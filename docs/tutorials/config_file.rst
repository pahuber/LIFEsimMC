.. _config_file:

Creating a Config File
======================

The following code shows the content of a LIFEsimMC config file ``config.py``. It contains a dictionary with the definitions
of the `Observation`, `Instrument` and `Scene` objects.

.. literalinclude::  ../_static/config.py
   :language: python